#include <gtest/gtest.h>
#include "jxx.lang.String.h"

using namespace jxx::lang;
/*
TEST(StringRegionMatches, CaseSensitiveAndInsensitive) {
    String a("HelloWorld");
    String b("HeLLo");
    EXPECT_TRUE(a.regionMatches(false, 0, String("Hello"), 0, 5));
    EXPECT_FALSE(a.regionMatches(false, 0, b, 0, 5));
    EXPECT_TRUE(a.regionMatches(true, 0, b, 0, 5));
}

TEST(StringRegionMatches, OffsetsAndLengths) {
    String a("abcXYZdef");
    String b("XYZ");
    EXPECT_TRUE(a.regionMatches(false, 3, b, 0, 3));
    EXPECT_FALSE(a.regionMatches(false, 4, b, 0, 3));
}
*/
